output_list = ["home"];
print(output_list);
for (i = 0; i < output_list.length; ++i) {
  neighbors = scan(output_list[i]);
  for (j = 0; j < neighbors.length; ++j) {
    if (!output_list.includes(neighbors[j])) {
      output_list.push(neighbors[j]);
    }
  }
}
tprint(output_list);
